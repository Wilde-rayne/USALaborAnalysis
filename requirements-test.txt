# Minimal dependency set to run the pytest suite without the heavy ML stack.
# Use in CI and lightweight dev containers. For full dev (linter etc.) pair
# this with requirements-dev.txt.
numpy>=1.26,<3.0
pandas>=2.2,<3.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0
hypothesis>=6.100,<7.0
pytest>=8.0,<10.0
pytest-cov>=5.0,<7.0
