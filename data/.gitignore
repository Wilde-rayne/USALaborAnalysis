# Materialized merge outputs — regenerated by utils/data_pipeline.
all_data.json
all_data.csv
all_data.parquet

# Raw series files fetched from BLS / Census. Too bulky and too volatile
# (re-downloaded on every force refresh) to version control.
raw/
