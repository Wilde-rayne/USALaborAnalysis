# Dev-only dependencies: test runner, linter, property-based testing.
# Keep lean so CI installs fast. Install with:
#     pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<10.0
pytest-cov>=5.0,<7.0
hypothesis>=6.100,<7.0
ruff>=0.4,<1.0
pyyaml>=6.0,<7.0
